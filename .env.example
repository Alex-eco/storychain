# Rename this file to .env and fill in the secrets. NEVER commit the real .env to Git.
# OpenAI API key (server-side)
OPENAI_API_KEY=sk-REPLACE_WITH_YOUR_OPENAI_KEY

# SQLite file (optional override)
DATABASE_URL=./db.sqlite

# Receiver crypto address for donations (frontend will use this)
RECEIVER_ADDRESS=0xREPLACE_WITH_YOUR_ETH_ADDRESS

# Optional: set PORT for backend
PORT=3000
